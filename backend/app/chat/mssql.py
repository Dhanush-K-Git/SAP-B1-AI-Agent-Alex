"""
DEPRECATED — direct MS SQL execution via pyodbc.

Superseded by app/chat/service_layer.py, which runs generated SQL through the SAP B1
service-layer HTTP endpoint (HANA-backed). Kept only as a reference; nothing imports it.
If you ever need a direct ODBC path again, restore from version control.
"""
